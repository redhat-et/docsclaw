tools:
  allowed:
    - exec
    - web_fetch
    - read_file
    - write_file
  exec:
    timeout: 30
    max_output: 50000
  workspace: /tmp/standalone-agent

loop:
  max_iterations: 10

# Uncomment to enable RAG search (requires a running Weaviate instance):
# rag:
#   backend: weaviate
#   url: http://localhost:8080
#   collection: Docs
#   text_field: content
#   default_limit: 5
#   max_limit: 20
